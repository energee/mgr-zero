# PROGRESS

Dated log of finished work, newest first, one line each; details belong in
commits and owning docs. What is left lives in `TODO.md`; what is in flight is
the open PR list. The dreaming workflow writes here from merged PR
descriptions, and `scripts/pr-directives.ts` moves finished `TODO.md` items
here.

## Done
- 2026-09-07 — Program 7 — taproom and kegs merged (needs 2) (#199)
- 2026-09-07 — Program 6 — purchasing merged (#198)
- 2026-09-07 — Program 5 — production and packaging merged (#194)
- 2026-09-07 — `PROGRESS.md` is the log only: Now dropped (in-flight work is the PR list, remaining scope is `TODO.md`). Its workstreams were finished (UI components port; Vercel readiness PR #29; audit P1 PR #27) or became programs in `TODO.md` (locations/bins → 2, sale channels → 4, purchasing → 6, Slice 1C → 13 and 15).
- 2026-09-07 — Screens design-refinement pass: `screens.tsx` audited end-to-end against schema/plan/brewing-domain (16 mismatches fixed, e.g. Receive PO now takes a lot code it used to draw read-only; 12 more flagged in DRIFT, PR #159); unit switchers unified into one segmented `Qty`/`TabBar` control and a command form's close now reads as a destructive discard (PR #160); row icons grew to 24px with an accent-ramp, WCAG-labeled corner status dot, and rail tabs (Today/Beer/Work/More) draw alike (PR #164); page actions hug their label on desk with list-create verbs on the title row (PR #174); Wholesale Shop lists packages by brand from each packaging run's wholesale-list designation, and buyers no longer see ATP (PR #175); Search and Entity picker gained keyboard selection via one shared `E.palette`, closing #99 (PR #182); Schedule batch draws Recipe and Brand as optional per the nullable schema and revision-2 intent-vs-commitment design (PR #172).
- 2026-09-07 — HTTP API reference moved from README to generated `/docs/api`, and its maintenance is now automated: `bun run docs:api` derives available operations from the command registry and designed operations from screens' `reads`/`writes`, generated curl examples are round-tripped through each command's own Zod schema so none fail validation, and the `http-api` agent now runs post-merge (`http-api-agent.yml`) restricted to prose/annotation edits (PRs #165, #166, #167).
- 2026-09-07 — Claude Code Review's bot-author skip generalized from two hardcoded branch names to any PR whose author `type` is `Bot`, after the old check missed a `mgr-bot`-authored PR and failed CI outright (PRs #169, #170).
- 2026-09-07 — Purchasing design decided: PO transport (direct/mailto/external) as a property rather than a `po_status` fork, vendor-owned observed lead time, four-number contract drawdown, damage-only capture at packaging-run close (PR #173).
- 2026-09-07 — `accent1-6` now resolve per theme instead of being frozen to the dark palette in light mode; Cancel order drawn destructive to match its sibling Confirm order (PR #161).
- 2026-09-06 — Locations, bins, and an internal stock-transfer document designed: `stock_transfers` stays a document distinct from priced customer `orders`, bin-to-bin moves are structurally barred from becoming deliveries (PR #158).
- 2026-09-07 — AI chat design consolidated into its own spec + plan, superseding slice 1C's composer tasks; chat scope is exactly `listTools({aiOnly:true})` by role. Companion decisions: staff roles become an array, Style gets its own per-brewery table, portal customers get a gated brand+week projection of planned batches (PR #180).
- 2026-09-07 — Documentation-maintainer prompt gained a `## Voice` rule (plain, casual, no em dashes, leave a correct sentence alone) and a proof-before-deletion guard, after a rewrite pass briefly deleted a still-live portal guide section (PRs #181, #184, #187).
- 2026-09-06 — Theme accent colors given fixed semantic status/action roles across the six-color palette (PR #156): confirm green, put-back/assign orange, pick/count/resume cyan, general purple, destructive pink/red, replacing identical ghost buttons on shared screen-inventory row actions; light/dark AA text-contrast tests added, both customer guides document the meanings. Sign out reclassified from the teal "irreversible-but-recording" accent to destructive, moving with the written convention in `app/globals.css` (PR #155).
- 2026-09-06 — Second audit remediation round (evidence was ephemeral `docs/audits/2026-09-05/*.md`, not committed): unknown order/customer/invoice/portal-order ids render the not-found page instead of a raw PostgREST or zod error, and every other database error is a generic 500 with the real error logged server-side (`lib/mgr/not-found.ts`, PR #144); every tenant table's `brewery_id` RLS predicate is index-backed and policies wrap `auth.uid()` in `(select ...)` (PR #149, gated by `tests/schema-rls-indexes.test.ts`); command forms announce submit failures and warnings to screen readers via `role="alert"`/`role="status"` (PR #146); New Order's Create button and the portal cart's Save draft/Submit stay disabled until the input is actually submittable, and the cart syncs edits into its saved draft before submit (PRs #145, #143); the nav rail and phone tabs hide entries whose target page doesn't exist yet instead of linking to nothing (PR #150); the unused `COMMAND_RATE_LIMIT_HMAC_SECRET` was deleted — rate limiting on `/api/command` remains unimplemented and README now says so (PR #147); assorted staff/portal guide and `ARCHITECTURE.md` drift corrected (PR #148).
- 2026-09-06 — `.agents/superpowers/specs/2026-09-05-mgr-packaging-source-planning.md`: a packaging run now names its product while planned (`product_id not null`) and only needs one exact source occupancy once started (`started_at is null or occupancy_id is not null`), so runs can be planned for a beer with no tank yet; amends the "FG lots" row in the schema-decisions doc. Not yet migrated — lands with the rest of the settled interface.
- 2026-09-04 — Design pass across `components/mgr/screens.tsx` (PR #132): an option's case now follows the word, not the control drawing it (a proper noun/named record is Title case, a generic domain term lowercase, a phrase sentence case — stated in the file header); the restock loop closes (`confirm_restock` clears `needs_restock` on put-back; short-shipping and cancelling a staged order set the same flag; a short pick that keeps its remainder no longer finishes the pick); Delivered joins the order lifecycle; every gated verb now states what its gate blocks; six previously spec-less records (return/credit, cycle count, planning, lot trace, pick sheet, search) get the policy they were deciding by omission.
- 2026-09-04 — Dream commits are attributed to `mgr-bot`, not `claude[bot]`: `claude-code-action` rewrites the repo's git identity while the model runs, so `dreaming.yml`'s validate step now commits with an explicit `git -c user.name=... -c user.email=...` naming the App slug, which no later config write can override. PR #131.
- 2026-09-03 — Session orientation formalized: `.agents/skills/orient` (report worktree, branch, status, PR base, then wait for confirmation) is operating-loop step 0; `AGENTS.md` Authority gained a rule against moving, renaming, or deleting files outside a request's explicit scope. PR #119.
- 2026-09-03 — Feature PRs stop editing `.agents/PROGRESS.md`/`MEMORY.md`/`DRIFT.md` directly — every PR inserting at the top of the same log conflicted with the next one. The PR description now carries the one-line progress note and any durable decision; the dreaming workflow reads merged PRs and writes these logs serially on its own `dreaming/main` PR. PR #105.
- 2026-09-03 — `design` (PR #69): **Packaging runs** (the Work list with the runs chip active: Upcoming by planned date with the next action on each row, a short Recent with lot/output/yield, Schedule run on top) and **Schedule packaging run** (sheet: date, one exact source occupancy, planned outputs per format with the barrels they consume, BOM material preview with shortages) drawn; Close packaging run keeps only the close half and links to the sheet from its Plan row. 85 MGR frames + 17 venue frames = 102. Hugeicons through one `Icon` wrapper; screen names drop their area prefix so the `/docs/screens` sub-index reads clean; Mobile/Desktop viewport switch (Desktop fills the docs column; Integrations pinned to desktop); `E.hd("Back · X", title)` draws an arrow link above a real title; screen copy carries no em dashes or machine names (test-enforced across body, job, states and spec); redrawn-screen reasoning and reads/writes fold into `<details>` on the docs page.
- 2026-09-02 — Package manager is Bun (`bun.lock`, `.bun-version` 1.3.10). CI and docs install/run with `bun`; Node 22 stays the Next.js runtime. `package-lock.json` is gone.
- 2026-09-02 — Dreaming follow-ups from the PR #62 review: the model job commits the dream and ships a `git bundle` (publish no longer re-merges main or 3-way applies a patch whose preimage blobs it may lack), the publish push is `--force-with-lease` on the `dreaming/main` head maintain saw, a rebuilt `dreaming/main` is logged and named in the PR body, `git merge --abort` no longer trips `set -e`, both Claude workflows share `.github/claude-ci-settings.json` (the dream reads main's copy, not the checked-out dream branch's), every dream commit including merges is authored by `mgr[bot]` via a metadata-only App token in the model job, the workflow test parses the YAML for real, and the PR #30 regression assertions are back. Known, deferred: a merge-commit merge of the dream PR still starts one empty paid run; the last-checked fetch is unauthenticated (fine while the repo is public).
- 2026-09-02 — Dreaming hardened after PR #60 review: the model job ships a `git diff` patch that publish applies `--3way` (concurrent main edits survive), a conflicting `dreaming/main` is rebuilt from main instead of failing every cron, the model job carries the documentation agent's deny list / `persist-credentials: false` / `show_full_output: false` and a `GH_TOKEN`, a merged dream no longer triggers an empty paid run, and the `gh api` bot-id lookup aborts on failure. Digests are optional dream input (the remember plugin retires them locally).
- 2026-09-02 — Audited all six merged `docs:` PRs: made portal recovery honest about ambiguous submit commits, removed the duplicate stale HTTP API section and pinned its catalog to the runtime registry, hardened generated HTML against external redirects/styles, flagged the obsolete Slice 1C QBO and incomplete sale-channel plans for revision, and corrected stale audit/browser references.
- 2026-09-02 — Customer guides are Fumadocs MDX in `content/docs` (`lib/source.ts`, `app/(docs)/docs`, `/api/search`); `fumadocs-ui/css/shadcn.css` maps the docs theme onto the app tokens, and `RootProvider` runs with its theme handling off so the app's `.dark` boot stays authoritative. Legacy `/docs/*.html` and `/docs/user-guide` URLs redirect (PR #67). Same-day dead ends superseded on the way here: a static `public/docs/user-guide.html` master (PR #57), then in-app body fragments styled by `.guide` rules (PR #63).
- 2026-09-02 — Dreaming is daily/manual and deterministic: Claude has read-only GitHub access and edits six agent documents, including durable `.agents/DRIFT.md`; a separate job validates and publishes as `mgr[bot]`, refuses PR-state races, and advances `last-checked` only after success. Lost PR #30 chat ownership/routing/API documentation was restored; MEMORY excludes test-owned implementation trivia.
- 2026-09-02 — Dropped `papaparse`/`@types/papaparse`: no references anywhere in the tree since audit P1.9 replaced the per-kind CSV importers with a fail-closed `import_csv`. PR #52.
- 2026-09-02 — Fixed a chat pacing bug: `paceConversation` stamped `lastSend` before the send instead of after, so the enforced interval was measured from the wrong edge and could undercount by a few ms under load — intermittent CI failures on `tests/chat-jobs.test.ts`. The limiter moved to `lib/chat/pacing.ts` (out of `lib/chat/jobs.ts`, the service-role client owner) and now waits in a loop until the deadline is actually reached rather than trusting one `setTimeout`, so the guarantee holds regardless of timer accuracy. Pinned by `tests/chat-pacing.test.ts`. PR #51.
- 2026-09-02 — Dream PRs are confirmed running as `mgr[bot]` end to end (MGR GitHub App installed, `vars.MGR_APP_ID`/`secrets.MGR_APP_PRIVATE_KEY` set); the "Now" item tracking that setup is closed. PRs #44, #45.
- 2026-09-02 — Docs-agent PR now actually runs CI: the publish job mints the MGR App token instead of `github.token`, whose pushes never trigger `ci.yml`; git identity comes from the app slug. Publish also stages before diffing so a guide the agent creates outright (not just edits) isn't silently dropped as "no changes". Slack `rowLabel()`'s substring `includes()` check (PR #40) could still swallow the reason inside a longer title; re-anchored with `endsWith`. `MgrIcon`'s `aria-label="MGR"` was on by default even though every call site already names MGR beside it, double-announcing to screen readers; decorative is now the default, label opt-in. PR #49.
- 2026-09-02 — Claude Code Review skips the bot-maintained `documentation/user-guide` branch; normal CI still runs while Claude no longer fails or reviews its own generated documentation PRs.
- 2026-09-02 — Documentation maintenance now runs only for PRs merged into `main`, and publishes from fresh `main`, preventing stacked-branch history from leaking into its generated PR.
- 2026-09-02 — Schema revision 2 as a design document (PR #47, §16 of `.agents/superpowers/specs/2026-08-31-mgr-schema-design.md` plus the revision-2 section of the decisions doc): brands, formats and composition, bins, sale-channel `tax_treatment`, channel-scoped price tiers, `pos_menus`, `keg_taps`, `repack`, `format_bom`, invoice drift. Twelve review findings reconciled in-doc (§16.16 lists what stays open). Nothing migrated — §16 lands in the baseline in one pass once the interface settles.
- 2026-09-02 — Wireframes redrawn for the POS and integration surfaces (73 → 100 frames): the venue filter gains QuickBooks and Square chrome, MGR's provider-neutral POS layer (Settings · Point of sale, Menu, POS item), the tap board with its swap sheet and open-keg audit, the taproom variance report, and a taproom Today landing; the count/POS inversion is settled — a physical count is the inventory source and POS supplies the expected number it is measured against. Rail gains Taps (Beer) and Menu (More), mirrored in the UI layout plan. PR #43.
- 2026-09-02 — Removed `.agents/orchestration/` (the Grok/Codex/Claude workflow router from #4/#8) and every reference to it: the `AGENTS.md` route and working-files entry, the `ARCHITECTURE.md` ownership row, the `MEMORY.md` cross-provider decision, and the `.mjs` include in `vitest.config.mts`. It never produced a run — `runs/` was empty for its whole life — so the docs were asserting a practice that did not exist. Vercel-plan Task 11 (signed external approval for its `--approve` gate, finding DOC08) is dropped with it, and contract 9 is now satisfied by deletion: no model-controlled approval path remains to sign. Recoverable from history if cross-provider orchestration is ever rebuilt.
- 2026-09-02 — Slack App Home rows no longer repeat the reason (`rowLabel()` appends the title only when the label doesn't already contain it) and the one call-to-action per surface (Link MGR account, message `open_mgr`, App Home footer) renders as Slack's primary button; found by drawing the Slack surfaces in Slack's own design language in the wireframes. PR #40.
- 2026-09-02 — `tests/chat-preview.test.ts` no longer reads the wireframes HTML as a drift guard (8 → 7 tests); the code-side guarantee (fixtures ↔ renderer) was already asserted independently. PR #38.
- 2026-09-02 — `agent-browser` is the only browser tool (gstack removed): `/browse` project skill (`.agents/skills/browse`, thin pointer to `agent-browser skills get core --full` + repo rules: slash-safe `--session`, ports, close), `.claude/skills` symlink, `AGENTS.md` step-4 eyeball check names it; `tests-e2e/portal-smoke.ts` header corrected (Chrome-only, lightpanda opt-in). Project `simplify` skill deleted — Claude Code's built-in `/simplify` and an inline `.pi/prompts/simplify.md` replace it. PR #37.
- 2026-09-02 — Customer documentation now has a stable, intentionally neutral `public/docs/user-guide.html` master linking separate staff and portal manuals; audited gaps are covered, and the three-file post-merge agent has a manual `main`-branch recovery trigger.
- 2026-09-01 — D1/D3 (`authz-d1-breweries-view`): customers no longer SELECT `breweries`; `portal_brewery` exposes `id`/`name`/`timezone`/`portal_fulfillment_location_id` only; `breweries.settings` is commented staff-only. Proven by `tests/rls-tenancy.test.ts` and `tests/schema-rules.test.ts`.
- 2026-09-01 — Audit P1 authorization remediation (`audit-p1-authz`): every staff mutation is an invoker RPC gated by `require_authorized_staff_rpc` with RLS write policies keyed on the PostgREST request path; Data API grants are explicit (`auto_expose_new_tables = false`) and pinned by catalog tests; QBO/POS tokens live in `private.integration_tokens` behind `lib/supabase/integration-tokens.ts` with purge triggers; portal orders use dedicated `portal_*` RPCs that derive tenant/customer/source/price/actor (omitted PO/note preserved); `next_no` is unreachable from the Data API (`set_doc_no` runs as owner, counter keys checked); `import_csv`/`invite_*` fail closed with their UI removed; slice 1C plan rewritten against the new boundary. 136 tests, `tsc`, lint, `db lint` clean.
- 2026-08-31 — Dreaming workflow (spec: `.agents/superpowers/specs/2026-08-31-dreaming-design.md`) reached working order after four post-merge fixes: docs-agent dropped the top-level `allOf` from `--json-schema` (claude-code-action's strict tool schema rejects it, 400); dreaming self-redispatches a main `push` as `workflow_dispatch` (the action rejects the `push` event) and allowlists the `github-actions` bot actor those redispatches run as; the CI-only guard is now stated directly in the workflow prompt because the tool allowlist has no `echo`/`env` for the agent to check `GITHUB_ACTIONS` itself. This PR is its first live run.
- 2026-08-31 — CI trims `supabase start` to the services tests touch (Postgres, Auth, PostgREST, Kong, mailpit — Auth invite emails need mailpit's local SMTP) and drops the redundant `supabase db reset`; `claude-code-review.yml` allowlists `Skill` for the code-review plugin's sub-skills; `claude.yml` drops the redundant `pull_request_review` trigger (every inline comment already fires `pull_request_review_comment`).
- 2026-09-01 — Claude Code Review skips `dreaming/main` PRs (job skipped, not failed); the action rejects non-human PR authors and we don't want Claude reviewing its own doc-curation PR.
- 2026-08-31 — Fixed both post-merge agent workflows: documentation agent dropped the top-level `allOf` from `--json-schema` (API 400: strict tool schemas reject oneOf/allOf/anyOf); dreaming self-redispatches a main push as `workflow_dispatch` because claude-code-action rejects the `push` event.
- 2026-08-31 — Slice 1B (orders) complete on `slice1b-orders`: order lifecycle (create/submit/confirm/adjust/cancel), allocations, pick/ship, per-shipment invoices, credit memos, taproom replenishment, customer/ship-to/price-list CRUD, wholesale customer portal (shop, orders, invoices), agent-browser e2e smoke (`npm run test:e2e`). Branch awaiting PR.
- 2026-08-31 — Wireframes raised 50 → 63 for the slice 1/1B exception paths: short pick, order detail (+restock), role Today landings (sales/brewer/driver), three ship bodies (invoice now / on-delivery gated / complete transfer), Product/SKU TTB facts, cellar addition, portal Review, Me, Team, set-password; existing frames patched (Square-vs-count rule, POS refunds, keg return + deposit refund, PO send/over, blend into occupied, recipe stage/timing, TTB file checks, print labels); plan §1–§4/§8 + `brewing-domain.md` yeast/water deferral in step.
- 2026-08-31 — Customer user guide established as the complete plain-language manual for every available screen/action; post-merge Claude audit enforces action steps, fields/options, permissions, results, corrections, and errors.
- 2026-08-31 — Read-only Claude Code post-merge documentation agent audits each merged PR against the owning docs and opens one idempotent follow-up issue for high-confidence drift; review tools are denied common runner secret paths and credential dotfiles.
- 2026-08-31 — `http-api` agent keeps README § HTTP API in lockstep with registered commands (`/http-api`).
- 2026-08-31 — Public API is the existing command endpoint; Bearer Supabase access tokens accepted alongside cookie sessions (`tests/api-command.test.ts`).
- 2026-08-31 — Slice 1B orders design spec (`2026-08-31-mgr-slice1b-orders-design.md`) and slice 1C implementation plan (`2026-08-31-slice1c-qbo-ai-chat.md`: QBO invoices-out/payments-back + AI chat composer, 11 TDD tasks in two independent tracks) written; PR #11.
- 2026-08-31 — Rev-3 simplification pass on latest main: actionable Today/Work ordering, Beer as a landing page, complete movement intents, simpler order state, separated taproom tasks, explicit cellar reading/remainder semantics, richer irreversible previews, and filing copy that does not imply transmission.
- 2026-08-31 — Recipe development loop designed: version snapshots assumptions + per-ingredient extract; OG/FG/ABV computed by one registry-layer formula (live preview + server reads; never stored, no view); per-batch actuals derived from readings via get_recipe_outcomes shown as deltas. Derived-values principle recorded in schema-decisions.
- 2026-08-31 — Wireframes + plan corrected per frontend review: frames draw only user-visible UI (state chips, gate names, tap audits moved to under-frame annotations), human copy for gated actions, named row verbs replace universal "Open", Today redrawn role-filtered as full-size 16px exemplar with ship-scale callout, hairline surfaces, cellar tiles lead with fill/occupancy, portal Order is a buyer catalog ("Ships from Warehouse", 48×48 steppers), composer proposal leads with signed effect, desk rail hierarchy + one-column default, safe-area on both shells, header Search/Me controls; plan §preamble/§3/§4/§5/§5b updated in step.
- 2026-08-31 — Wireframes: phone/desk toggle renders all 50 frames as desktop views from the same bodies (rail/top-nav shells, dialog sheets, 32 px cursor-density controls); plan §1 reference updated.
- 2026-08-31 — Fresh-eyes UI plan rev 3 + rendered artifact: honest persona tap audits, proposal safety, exact registry/atomicity contracts, complete portal/nav/state coverage, wet-phone tokens, entry-first build order, and explicit implementation/schema gates; v1 CONFLICT/REPEAT cross-check folded in; PR #3's 11 gap screens reconciled: consolidated into rev-3 frames or dropped per plan decisions (Notifications history, portal writes).
- 2026-08-31 — Wireframes: 11 gap screens added (settings sub-pages, new order/PO, customer/product/price-list editors, recipe editor, portal account, keg return); later subsumed by rev 3.
- 2026-08-31 — Pi `/simplify` alias and harness-compatible `.agents/skills/simplify` workflow for behavior-preserving cleanup of recently modified code.
- 2026-08-31 — Harness-neutral multi-model workflow in `.agents/orchestration/`: tiered Grok/Codex/Claude routing, hard run budgets, approval gates, read-only reviewers, run artifacts, and dependency-free tests.
- 2026-08-31 — Wireframes for all 50 planned screens (`2026-08-31-mgr-wireframes.html`, published as artifact); UI plan rev 2 with 13 added flows and entry-first build order.
- 2026-08-31 — UI layout plan (mobile-first, composer + forms, all ten slices): `2026-08-31-mgr-ui-layout-plan.md`, draft awaiting Ted.
- 2026-08-31 — Docs reconciled to the baseline (README test list, rule count, stale skip notes, v1-review status, proxy migration).
- 2026-08-31 — `tests/schema-rules.test.ts` (pg_catalog gates), all rules active; definer fns revoked from anon/public in the baseline.
- 2026-08-31 — Single baseline migration `00001_baseline.sql` (58 tables, all ten slices) replaces the two slice-1A migrations; design in `2026-08-31-mgr-schema-design.md`; `tests/schema-conventions.test.ts` proves composite FKs, lot trigger, append-only ledgers.
- 2026-08-31 — Reviewed MGR v1; adopted iron rule 5 (multi-write = one plpgsql fn, `tests/write-atomicity.test.ts`) and `brewing-domain.md`. Remaining pulls listed in the review doc §3.
- 2026-08-31 — Schema design decisions doc for the single-baseline migration.
- 2026-08-31 — Slice 1A (foundation) complete on `slice1a-foundation`: auth, app shell, catalog, inventory ledger/ATP, CSV import, invitations, CI. PR #1 open, CI green.
