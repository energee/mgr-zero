# Dreaming Implementation Plan

**Status: superseded — shipped 2026-08-31.** The committed artifacts are
authoritative; the drafts once inlined here drifted from them and were
removed so a re-run can't follow stale copies:

- Workflow: `.github/workflows/dreaming.yml`
- Prompt contract: `.agents/agents/dreaming.md`
- Digest tracking: `.gitignore` (`.remember/today-*.md` allowlist)
- Design rationale: `.agents/superpowers/specs/2026-08-31-dreaming-design.md`

## Remaining: live test (after merge to main)

```bash
gh workflow run dreaming.yml && sleep 90 && gh run list --workflow=dreaming.yml --limit 1
```

Verify the run against the checklist in the spec's Testing section
(`.agents/superpowers/specs/2026-08-31-dreaming-design.md`) — that section
is authoritative for the verification criteria and the known `github.token`
CI-suppression limitation. Report results to the user.
