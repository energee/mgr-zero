// lib/mgr/fixtures/first-run.ts — First-run checklist snapshot.
import type { FirstRunViewModel } from "@/lib/mgr/first-run-view";

export const firstRunDemo: FirstRunViewModel = {
  brewery: "Demo Brewing",
  steps: "5 steps",
};
